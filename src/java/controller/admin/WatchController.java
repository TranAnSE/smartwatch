/* * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license * Click nbfs://nbhost/SystemFileSystem/Templates/JSP_Servlet/Servlet.java to edit this template */package controller.admin;import dal.WatchDAO;import model.Watch;import jakarta.servlet.ServletException;import jakarta.servlet.http.HttpServlet;import jakarta.servlet.http.HttpServletRequest;import jakarta.servlet.http.HttpServletResponse;import java.io.File;import java.io.IOException;import java.io.PrintWriter;import java.util.HashMap;import java.util.List;import jakarta.servlet.http.Part;import java.io.InputStream;import java.nio.file.Files;import java.nio.file.Paths;/** * * @author an */public class WatchController extends HttpServlet {    /**     * Processes requests for both HTTP <code>GET</code> and <code>POST</code>     * methods.     *     * @param request servlet request     * @param response servlet response     * @throws ServletException if a servlet-specific error occurs     * @throws IOException if an I/O error occurs     */    protected void processRequest(HttpServletRequest request, HttpServletResponse response)            throws ServletException, IOException {        response.setContentType("text/html;charset=UTF-8");        try (PrintWriter out = response.getWriter()) {            /* TODO output your page here. You may use following sample code. */            out.println("<!DOCTYPE html>");            out.println("<html>");            out.println("<head>");            out.println("<title>Servlet WatchController</title>");            out.println("</head>");            out.println("<body>");            out.println("<h1>Servlet WatchController at " + request.getContextPath() + "</h1>");            out.println("</body>");            out.println("</html>");        }    }    // <editor-fold defaultstate="collapsed" desc="HttpServlet methods. Click on the + sign on the left to edit the code.">    /**     * Handles the HTTP <code>GET</code> method.     *     * @param request servlet request     * @param response servlet response     * @throws ServletException if a servlet-specific error occurs     * @throws IOException if an I/O error occurs     */    @Override    protected void doGet(HttpServletRequest request, HttpServletResponse response)            throws ServletException, IOException {        WatchDAO watchDAO = new WatchDAO();        String action = request.getParameter("action");        if (action == null) {            action = "list";        }        switch (action) {            case "add":                request.getRequestDispatcher("/admin/watch/add.jsp").forward(request, response);                break;            case "edit":                int id = Integer.parseInt(request.getParameter("id"));                Watch watch = watchDAO.getWatchByID(id);                request.setAttribute("watch", watch);                request.getRequestDispatcher("/admin/watch/edit.jsp").forward(request, response);                break;            case "delete":                id = Integer.parseInt(request.getParameter("id"));                watchDAO.deleteWatch(id);                response.sendRedirect(request.getContextPath() + "/admin/list-watches");                break;            case "list":            default:                List<Watch> watches = watchDAO.getAllWatches();                request.setAttribute("watches", watches);                request.getRequestDispatcher("/admin/watch/list.jsp").forward(request, response);                break;        }    }    /**     * Handles the HTTP <code>POST</code> method.     *     * @param request servlet request     * @param response servlet response     * @throws ServletException if a servlet-specific error occurs     * @throws IOException if an I/O error occurs     */    @Override    protected void doPost(HttpServletRequest request, HttpServletResponse response)            throws ServletException, IOException {        String action = request.getParameter("action");        WatchDAO watchDAO = new WatchDAO();        if ("add".equals(action)) {            String name = request.getParameter("watchName");            String description = request.getParameter("description");            String thumbnail = request.getParameter("thumbnail");            int price = Integer.parseInt(request.getParameter("price"));            String trademark = request.getParameter("trademark");            int categoryID = Integer.parseInt(request.getParameter("categoryID"));            Watch newWatch = new Watch(0, name, thumbnail, description, price, trademark, categoryID);            watchDAO.addWatch(newWatch);        } else if ("edit".equals(action)) {            int id = Integer.parseInt(request.getParameter("watchID"));            String name = request.getParameter("watchName");            String description = request.getParameter("description");            String thumbnail = request.getParameter("thumbnail");            int price = Integer.parseInt(request.getParameter("price"));            String trademark = request.getParameter("trademark");            int categoryID = Integer.parseInt(request.getParameter("categoryID"));            Watch updatedWatch = new Watch(id, name, thumbnail, description, price, trademark, categoryID);            watchDAO.updateWatch(updatedWatch);        }        response.sendRedirect(request.getContextPath() + "/admin/list-watches");    }    /**     * Returns a short description of the servlet.     *     * @return a String containing servlet description     */    @Override    public String getServletInfo() {        return "Short description";    }// </editor-fold>}